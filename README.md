# mark-releases

This repository exists solely to host releases and the appcast for [Mark](https://github.com/whomwah/mark) — a native macOS markdown editor.

- **GitHub Releases** — holds the `.dmg` release assets
- **`gh-pages` branch** — serves the `appcast.xml` used by Sparkle for auto-updates

Do not commit source code here.
